# Dependências de runtime do microsserviço inventory (Aula 5 + 6).
# Aula 6: persistência real em PostgreSQL via SQLAlchemy 2.0, com o esquema
# versionado pelo Alembic. O inventory compartilha o banco com o Django, porém
# governa apenas as próprias tabelas (o Alembic nunca toca nas do Django).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0